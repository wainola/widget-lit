{
  "name": "@wainola/lit-widget",
  "version": "4.0.1",
  "main": "build/index.js",
  "module": "build/index.js",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@buildwithsygma/sygma-sdk-core": "^2.2.0",
    "@wainola/lit-sdk-manager": "2.0.2",
    "@wainola/lit-wallet-manager": "2.1.0",
    "ethers": "5.7.2",
    "lit": "^2.8.0"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc --build ./tsconfig.json",
    "watch": "tsc --build --clean && tsc --build ./tsconfig.json --watch",
    "clean": "rm -rf ./build",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "yarn run lint --fix"
  }
}
