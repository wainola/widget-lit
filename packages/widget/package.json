{
  "name": "@wainola/lit-widget",
  "version": "1.0.0",
  "main": "build/index.js",
  "module": "build/index.js",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "lit": "^2.8.0",
    "@wainola/wallet-manager": "^1.0.0",
    "@buildwithsygma/sygma-sdk-core": "^2.2.0"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc --build ./tsconfig.json",
    "watch": "tsc --build --clean && tsc --build ./tsconfig.json --watch",
    "clean": "rm -rf ./build",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "yarn run lint --fix"
  }
}
