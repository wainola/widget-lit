{
  "name": "@wainola/react-widget",
  "version": "1.1.0",
  "main": "build/index.js",
  "module": "build/index.js",
  "license": "MIT",
  "dependencies": {
    "@lit-labs/react": "2.0.3",
    "@wainola/lit-widget": "2.0.1"
  },
  "peerDependencies": {
    "react": ">=17",
    "react-dom": ">=17"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc --build ./tsconfig.json",
    "watch": "tsc --build --clean && tsc --build ./tsconfig.json --watch",
    "clean": "rm -rf ./build",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "yarn run lint --fix"
  }
}
