{
  "name": "@wainola/lit-wallet-manager",
  "version": "2.0.0",
  "main": "build/index.js",
  "module": "build/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build --clean && tsc --build ./tsconfig.json",
    "watch": "tsc --build --clean && tsc --build ./tsconfig.json --watch",
    "clean": "rm -rf ./build",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "yarn run lint --fix"
  },
  "devDependencies": {
    "@polkadot/types": "10.7.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@polkadot/api": "10.7.2",
    "@polkadot/extension-dapp": "^0.46.5",
    "ethers": "5.7.2"
  }
}
