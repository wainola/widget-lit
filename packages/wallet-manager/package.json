{
  "name": "@wainola/wallet-manager",
  "version": "1.0.0",
  "main": "build/index.js",
  "module": "build/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build --clean && tsc --build ./tsconfig.json",
    "watch": "tsc --build --clean --watch && tsc --build ./tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@polkadot/api": "^10.9.1",
    "@polkadot/extension-dapp": "^0.46.5",
    "ethers": "5.7.2"
  }
}
