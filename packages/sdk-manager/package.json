{
  "name": "@wainola/lit-sdk-manager",
  "version": "2.0.2",
  "main": "build/index.js",
  "module": "build/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build --clean && tsc --build ./tsconfig.json",
    "watch": "tsc --build --clean && tsc --build ./tsconfig.json --watch",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "yarn run lint --fix",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@polkadot/types": "10.7.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@buildwithsygma/sygma-sdk-core": "^2.2.0",
    "@ethersproject/contracts": "^5.7.0",
    "@ethersproject/transactions": "^5.7.0",
    "@polkadot/api": "10.7.2",
    "@wainola/lit-wallet-manager": "2.1.1"
  }
}
